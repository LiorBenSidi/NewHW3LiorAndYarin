/**
 * An enumeration representing the scanning order for a playlist.
 */
public enum ScanningOrder {
    ADDING, // Specifies the scanning order where songs are added to the playlist.
    NAME, // Specifies the scanning order based on the name of the songs.
    DURATION // Specifies the scanning order based on the duration of the songs.
}
